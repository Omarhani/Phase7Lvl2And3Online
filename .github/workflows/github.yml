name: Regression


on:
  push:
    branches:
      - 'main'
    pull-request:
      branches:
      - 'main'

jobs:
  Regression:
   runs-on: ubuntu-latest

   steps:
    - name: Regression
      run: mvn test -Dtestng=regression
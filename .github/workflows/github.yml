name: Regression
  
  - uses: actions/checkout@v3
    with:
     path: /Users/omarhany/IdeaProjects/Phase7Lvl2And3Online

on:
  push:
    branches:
      - 'main'
    pull-request:
      branches:
      - 'main'

jobs:
  Regression:
   runs-on: ubuntu-latest

   steps:
    - name: Regression
      run: mvn test -Dtestng=regression

